name: uptime
title: A project to monitor the status of servers.
publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  go_router: ^6.0.7
  firebase_auth: ^4.2.9
  firebase_core: ^2.7.0
  flutterfire_ui: ^0.4.3+20
  flutter_riverpod: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
  - images/
 